# 网关配置。复制成 .env 后填写: cp .env.example .env

# ---- 域名 ----
# 在 DNS 上把这条 A 记录指向服务器 IP，例如:
#   类型 A   主机记录 token   值 你的服务器公网 IP
#
# 域名还没买的话先留着默认值别动，能跑通前面的流程（用自签证书），
# 只是签不了正式证书。等买了域名再回来改。
TOKEN_HUB_DOMAIN=token.example.com

# ---- Let's Encrypt ----
# 证书到期提醒和紧急通知会发到这里。
ACME_EMAIL=admin@example.com

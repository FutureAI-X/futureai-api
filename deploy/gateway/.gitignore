# 网关运行时生成的目录，一律不进版本控制。
#
# 这些目录平时是空的（由脚本或 Docker 在首次使用时创建），
# 但它们一旦有内容，就都是不该提交的东西：

# 证书与私钥。
# 自签阶段是自签私钥，换成正式证书后就是**线上域名的真实私钥**——
# 泄露它等于任何人都能冒充你的站点做中间人。
certs/

# Let's Encrypt 的工作目录：
#   conf/  签发后的证书与私钥（live/ 下是私钥本体）
#   www/   ACME HTTP-01 校验用的临时文件
certbot/

# 访问日志。含客户端 IP 与请求路径，不该进仓库。
logs/

# 域名等部署参数（根 .gitignore 已覆盖，这里再声明一次以免将来误改）
.env
